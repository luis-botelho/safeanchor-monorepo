generator client {
  provider = "prisma-client"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "postgresql"
}

model Vessel {
  id     String @id @default(cuid())
  name   String
  type   String
  status String
}